{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }

    </style>

    <a href="{{ path('homepage') }}"
       class="button is-primary">
        To Blogs!
    </a>

    <div class="example-wrapper">
        <h1>✅ Blog Entry by user {{ post.author.email }} </h1>
        <h3> {{ post.title }} </h3>
        <div>{{ post.body }}</div>

    </div>
{% endblock %}
